---
---

No release. This branch rewrites the docs, wraps them at 100 columns, and
repairs CI. The one change under `packages/` is a doc comment in
`@concile/tui` that pointed at `website/` after the rename to `www/`. No
published behavior changes, so this is an empty changeset rather than a
patch bump across the fixed group.
