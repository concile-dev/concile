// Same card as Open Graph. Twitter/X reads twitter:image first, so without
// this file it falls back to nothing on some clients.
export { default, alt, size, contentType } from './opengraph-image';
